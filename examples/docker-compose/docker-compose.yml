x-volumes:
  copyparty:
    image: copyparty/ac:latest
    container_name: copyparty
    restart: always
    user: "1000:1000"
    ports:
      - "3923:3923"
    volumes:
      - ./cfg:/cfg
      - "${FILESHARE_LOCAL_PATH}:/w"
      - ./handlers:/handlers

      - /etc/timezone:/etc/timezone:ro
      - /etc/localtime:/etc/localtime:ro
    tmpfs:
      - /tmp

    environment:
      LD_PRELOAD: /usr/lib/libmimalloc-secure.so.NOPE # enable mimalloc by replacing "NOPE" with "2" for a nice speed-boost (will use twice as much ram)
      PYTHONUNBUFFERED: 1 # ensures log-messages are not delayed (but can reduce speed a tiny bit)

      FPKGV_PS4_IP: "${FPKGV_PS4_IP}"
      FPKGV_CP_HOST: "${FPKGV_CP_HOST}"
      FPKGV_SEND_USERS: "${FPKGV_SEND_USERS}"

    stop_grace_period: 15s  # thumbnailer is allowed to continue finishing up for 10s after the shutdown signal
    healthcheck:
      # hide it from logs with "/._" so it matches the default --lf-url filter
      test: ["CMD-SHELL", "wget --spider -q 127.0.0.1:3923/?reset=/._"]
      interval: 1m
      timeout: 2s
      retries: 5
      start_period: 15s
